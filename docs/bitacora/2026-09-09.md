# Bitácora — 2026-09-09 — R2 Natalia

## HANDOFF — 2026-09-09 03:54 UTC — R2 — Codex → siguiente sesión

**Tickets:** A5 y A8, apoyo a A6 y pruebas propias de A15. Ninguna tarea completa se cierra con este incremento.

**Rama:** `R2-Natalia-Hernandez`, local. Git no permite los dos puntos ni los espacios del nombre originalmente solicitado.

**Commit de referencia previo a este handoff:** `6ea498f`. La base del inicio era `748d52d`; la base remota revisada era `3859d6a` en `main`.

**Estado verificado:** la demo de práctica recorre dos rondas, votación y revelación, y guarda una simulación en SQLite. El dominio tiene ventana configurable. El cliente consume gRPC con deadline y cierre explícito, rechaza turnos inválidos antes de invocar y descarta streams parciales. Los fallos técnicos revelan la IA y no generan tasa de detección.

**Verificación realizada:** Python 3.13.15; 64 pruebas correctas (62 unitarias y 2 de integración gRPC por loopback), sin warnings. Ruff correcto; Black sin cambios. El proto compila y los stubs se importan. Cobertura total de `src`: 75 %; dominio 97 %, cliente 98 %, SQLite 100 %. La consola se ejecutó aparte y no suma a esa cobertura de pytest.

**Bloqueado por:** para integrar de verdad, faltan el servicio HTTP de R1, sus mediciones y la revisión conjunta del contrato. Para publicar esta rama, la revisión automática rechazó la operación de escritura por un límite de uso antes de llegar a GitHub. No se pudo comprobar si los nuevos permisos ya resuelven el acceso de escritura. Natalia ya autorizó publicar y pidió commits pequeños con explicación; la rama no está subida al cerrar esta sesión.

**Decisiones tomadas:** aplicar la instrucción de inferencia por API mediante el servicio gRPC de R1. El modelo no se aloja en el Droplet. ADR-001 registra esa instrucción y ADR-003 mantiene provisionales los parámetros de 20 s/8 s. No se incorporan fallback local, reintentos de aplicación en R2, pregeneración ni caché experimental.

**Archivos tocados:** `src/orchestrator/`, `proto/`, `tests/`, `scripts/validate_proto.py`, entorno UV, README y documentación. Se retiró la estructura anterior `r2_inicio/` del código versionado.

### Commits de esta adaptación

Cada commit tiene un título breve y un cuerpo que explica el cambio. Esta tabla no incluye el commit de este propio handoff.

| Commit | Cambio |
| --- | --- |
| `9985c95` | Estructura de R2, UV y ventanas de ronda. |
| `7069e6d` | Campos aditivos de API y stubs del contrato. |
| `ae339f7` | Cliente gRPC, deadline y validación del cierre. |
| `e15e74d` | Pruebas de errores remotos y transporte local. |
| `d63fe25` | Exclusiones y retiro de dependencias duplicadas. |
| `42068e6` | ADR de inferencia y propuesta de parámetros. |
| `09fd316` | Revisión del plan y verificación pública del proveedor. |
| `6ea498f` | Guía actualizada y acuerdos para integrar. |

### Cómo reproducir el estado actual

Desde la raíz de una copia que contenga esta rama:

```bash
uv sync --locked
uv run ruff check src tests scripts
uv run black --check src tests scripts
uv run pytest -q --cov=src --cov-report=term-missing
uv run python scripts/validate_proto.py
uv run python -m src.orchestrator.demo --auto
```

La simulación no llama a Hugging Face. Las dos pruebas de integración arrancan un servidor de prueba local; no acreditan integración con el engine real. La consulta pública de metadatos encontró un proveedor anunciado, pero no midió latencia ni costo.

**Siguiente acción concreta:** cuando se restablezca la capacidad de publicación, revisar la base remota y publicar la secuencia de commits mediante la integración autorizada. En el código, revisar con R1 la configuración, la cancelación HTTP al vencer gRPC, el cierre vacío y los metadatos efectivos; luego acordar con R3 el contrato del servidor multijugador. No asumir que 64 pruebas completan A8 o la meta global de 120.

**Callejones ya probados — no repetir:** el inicio aislado ya fue trasladado; no crear otra copia de `r2_inicio`. Pytest necesitó `pythonpath=["."]` en el entorno raíz. No generar stubs con imports planos: usar `-I.` desde la raíz. El intento anterior de publicar recibió un 403 de la integración; el intento más reciente quedó bloqueado antes de GitHub por la revisión automática. No tratar el límite de uso como una nueva denegación de permisos de la organización ni intentar eludirlo.

**Contexto mínimo para la próxima sesión:** `docs/REVISION_CAMBIO_API.md`, `docs/ACUERDOS_R2.md`, ambos ADR, `proto/impostor.proto` y `src/orchestrator/engine_client.py`. El temporizador activo, servidor, autenticación, concurrencia, persistencia de partidas en curso y MLflow todavía están pendientes.

## HANDOFF — 2026-09-09 04:01 UTC — reintento de publicación

**Estado verificado:** la base remota sigue en `3859d6a`; la rama R2 aún no aparece en GitHub. El árbol de trabajo estaba limpio y se preparó la publicación conservando todos los commits.

**Bloqueado por:** esta vez la solicitud llegó a GitHub y recibió `403: Resource not accessible by integration`. La cuenta tiene permisos `push` y `admin`, pero `uao-neumonia-loscracks` no aparece entre las instalaciones disponibles de la aplicación. El diagnóstico actual sustituye al límite de uso del intento anterior.

**Siguiente acción concreta:** habilitar la aplicación conectada en la organización para el repositorio `sospechAI`, con escritura de contenido, y después reintentar la publicación autorizada. No se creó una rama remota ni se modificó `main`.

**Callejones ya probados — no repetir:** esperar al límite de uso no resuelve el 403 actual. No repetir escrituras ni cambiar de identidad para eludir los permisos. El código y las pruebas no cambiaron en este reintento.
